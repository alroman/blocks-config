# A sample Guardfile
# More info at https://github.com/guard/guard#readme

# Monitor changes to SASS files
guard 'rake', :task => 'build_css' do
  watch(%r{^moodle/theme/uclashared/src/sass/.+\.scss$})
end
